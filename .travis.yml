language: go
go:
 - 1.2
 - 1.3
 - 1.4
 - release

install:
  - go get code.google.com/p/go.tools/cmd/vet
  - go install -race std

script:
  - go fmt ./...
  - go vet ./...
  - go test -x -v ./...
  - go test -i -race ./...
  - go test -v -race ./...
  - go get -d -v ./... && go build -v ./...